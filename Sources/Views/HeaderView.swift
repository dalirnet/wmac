import SwiftUI

struct HeaderView: View {
    var body: some View {
        Text("Welcome to WMac")
            .font(.largeTitle)
            .fontWeight(.bold)
            .foregroundColor(.blue)
    }
}
